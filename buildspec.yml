version: 0.2

phases:
  pre_build:
    commands:
      - echo "No pre build commands"
  build:
    commands:
      - make bootstrap
      - make test
  post_build:
    commands:
      - echo "No post build commands"
